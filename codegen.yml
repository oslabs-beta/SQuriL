overwrite: true
schema: "./server/schemas/input/inputSchema.graphql"
documents: null
generates:
  server/schemas/output/outputSchema.ts:
    plugins:
      - "typescript"
      - "typescript-resolvers"
  ./graphql.schema.json:
    plugins:
      - "introspection"
